# Problem: Write a program to calculate the area of a Triangle
# Triange formula - 1/2*base*height

height = float(input("Enter the area for height: "))
base = float(input("Enter the area for base: "))

area = 1/2*height*base #(0.5)

print("The area of the Triangle is ", area)